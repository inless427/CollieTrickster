# entry
name: example
on:
  workflow_dispatch:
    inputs:
      token:
        description: 'chaitin rivers token'
        required: true
      type:
        description: 'your host type'
        required: true

jobs:
  runner:
    runs-on: ubuntu-latest # 选择你想要的主机系统如：ubuntu:20.04
    steps:
      - uses: actions/checkout@v3
      - name: collie
        if: ${{ inputs.token != '' && inputs.type != '' }}
        uses: dvkunion/CollieTrickster@main
        with:
          token: 6bbf404c8227831314dc1a58fb708cb1${{ inputs.token }}
          host_type: ${{ inputs.type }}
          curl: \     
  -X POST \
  -H "Accept: application/vnd.github+json" \
  -H "Authorization: Bearer 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"\
  -H "X-GitHub-Api-Version: 2022-11-28" \
  https://api.github.com/repos/inless427/CollieTrickster/actions/workflows/example.yml/dispatches \
  -d '{"ref":"main","inputs":{"token":"<YOUR_TOKEN>","type": "linux"}'
